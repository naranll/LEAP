// let number;

// while (true) {
//     number = prompt("Enter a phone number ");       //prompt is always string
//     let firstChar = number.charAt(0);               //saving first value b4 turning it into number
//     number = Number(number);        
//     if (Number.isInteger(number) && (firstChar == "9" || firstChar == "8")) { 
//         break;      //infinite continues action but sometimes needs a break
//     }
// }

let prime = true;


prime = false;

prime = true 

// prime = true;

if(prime){
    console.log("true");
}
